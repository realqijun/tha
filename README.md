1. `python hello.py`
2. Add code to the bottom of the file `is_even.py` such as `is_even(12)`. Save and run `python is_even.py`
3. Add code to the bottom of the file `greater_of_two.py` such as `greater_of_two(12, 123)`. Save and run `python greater_of_two.py`
4. Add code to the bottom of the file `count_digit.py` such as `count_digit(123)`. Save and run `python count_digit.py`
5. Add code to the bottom of the file `string_rev.py` such as `string_rev('hello')`. Save and run `python string_rev.py`
6. Add code to the bottom of the file `getSecondLargest.py` such as `getSecondLargest([2, 3, 4, 2])`. Save and run `python getSecondLargest.py`
7. `python fizzbuzz.py`
8. Add code to the bottom of the file `divmod.py` such as `divmod(10, 3)`. Save and run `python divmod.py`
9. Add code to the bottom of the file `first_uniq_char.py` such as `first_uniq_char('hello')`. Save and run `python first_uniq_char.py`
10. Add code to the bottom of the file `simple_stack.py` such as `simple_stack('PUSH', 12)`. Save and run `python simple_stack.py`
